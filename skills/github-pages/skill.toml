[skill]
name = "github-pages"
version = "0.1.0"
description = "Publish the built static site to a GitHub Pages branch."

[config]
repo = { description = "Git remote URL for the GitHub Pages repository.", required = true }
branch = { description = "Publish branch.", required = false }
