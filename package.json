{
  "name": "reef",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "reef": "./bin/reef.ts"
  },
  "scripts": {
    "reef": "bun run bin/reef.ts",
    "test": "bun test",
    "check": "bun test && bun build bin/reef.ts --target bun --outdir /private/tmp/reef-build"
  }
}
